// Add an event listener to the login button
document.getElementById('login-btn').addEventListener('click', function(e) {
    // Redirect the user to the platform.html page
    window.location.href = './platform.html';
});
